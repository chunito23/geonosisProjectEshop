<mvc:View
    controllerName="starwarsfrontend.controller.User"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
>
    <Page
        id="userPage"
        title="Perfil de Usuario"
    >
        <content>
            <VBox class="sapUiSmallMargin">
                <Text id="emailUsuario" class="sapUiSmallMarginBottom"/>
                <Button 
                    text="Volver a la pantalla principal" 
                    press=".onNavBack" 
                    class="sapUiSmallMarginTop"/>
            </VBox>

            <VBox >
                <Text text="items favoritos"></Text>
                <!-- <List items = "{/favoritos}"></List> -->
            </VBox>
        </content>
    </Page>
</mvc:View>